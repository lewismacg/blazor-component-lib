@namespace BlazorComponents

<i class="material-icons noselect @ColourMapper.GetColourClass(IconColour) @(IsLarge ? "large-icon" : string.Empty)" @onclick="@(() => OnClickAction.InvokeAsync())" id="@IconId">@ChildContent</i>